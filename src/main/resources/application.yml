spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/jooq_example?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: zigzag
  jooq:
    sql-dialect: mysql
  sql:
    init:
      enabled: true


graphql:
  tools:
    schema-location-pattern: "**/*.graphqls"
    introspection-enabled: true
  graphql:
    servlet:
      mapping: /graphql
      enabled: true
#      exception-handlers-enabled: true

